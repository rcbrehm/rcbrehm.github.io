source "https://rubygems.org"

# GitHub Pages builds the site automatically; this Gemfile is only needed
# for optional local previews (bundle install && bundle exec jekyll serve).
gem "github-pages", group: :jekyll_plugins
